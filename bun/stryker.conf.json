{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "StrykerJS has no official Bun test-runner plugin; the universal 'command' runner shells out to `bun test` and grades mutants by exit code. Advisory gate: `harness mutation` scores the JSON report itself, so no `thresholds.break` here.",
  "packageManager": "npm",
  "testRunner": "command",
  "commandRunner": { "command": "bun test" },
  "mutate": ["src/**/*.ts", "!src/**/*.test.ts"],
  "reporters": ["clear-text", "progress"],
  "jsonReporter": { "fileName": "reports/mutation/mutation.json" },
  "coverageAnalysis": "off",
  "tempDirName": ".stryker-tmp"
}
